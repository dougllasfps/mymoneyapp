import {Router, Route} from 'react-router'

export default class Rotas extends React.Component{
    render(){
        return (
          <Router>
            <Route  />
          </Router>
        );
    }
}